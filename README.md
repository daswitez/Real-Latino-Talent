# Growify Design Preview

Static design preview for the Growify landing page template.

Open `index.html` or publish the repository with GitHub Pages. The default preview redirects to:

`main_files/click-trough.html`

PHP email handlers and PHPMailer are intentionally excluded because this repository is only for visual preview.
